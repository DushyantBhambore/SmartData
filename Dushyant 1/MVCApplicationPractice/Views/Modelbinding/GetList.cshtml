@model List<StudentViewModel>

@if (Model is not null && Model.Any())
{
@* 
    <h2> usig For loop </h2>

    <form method="post" asp-controller="Modelbinding" asp-action="GetList">
        <table class="table table-success table-hover ">
            <thead>
                <tr>
                    <th>StuId</th>
                    <th>FirstName</th>
                    <th>LastName</th>
                    <th>Email</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>
                        <input asp-for="@item.StuId" />
                        </td>
                        <td>
                            <input asp-for="@item.FirstName" />
                        </td>
                        <td>
                            <input asp-for="@item.LastName" />
                        </td>
                        <td>
                            <input asp-for="@item.Email" />
                        </td>

                    </tr>

                }

            </tbody>
            
        </table>
        <button type="submit"  class="btn btn-outline-primary">Submit    </button>

    </form> *@

    <h2> usig For loop </h2>
    <form method="post" asp-controller="Modelbinding" >
        <table class="table table-success table-hover ">
            <thead>
                <tr>
                    <th>StuId</th>
                    <th>FirstName</th>
                    <th>LastName</th>
                    <th>Email</th>
                </tr>
            </thead>
            <tbody>
                @for(int i=0;i<Model.Count;i++)
                {
                    <tr>
                        <td>
                            <input asp-for="@Model[i].StuId" />
                        </td>
                        <td>
                            <input asp-for="@Model[i].FirstName" />
                        </td>
                        <td>
                            <input asp-for="@Model[i].LastName" />
                        </td>
                        <td>
                            <input asp-for="@Model[i].Email" />
                        </td>

                    </tr>

                }

            </tbody>

        </table>
        <button type="submit"  class="btn  btn-outline-primary">Submit</button>


    </form>


}
else
{
    <h1> No Record Found </h1>
}